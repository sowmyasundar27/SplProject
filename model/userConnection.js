var userConnection = function(connection, rsvp){
var userConnectionInfo = {usersConnection:connection,
  userConnectionrsvp:rsvp};
return userConnectionInfo;
};

module.exports.userConnection = userConnection;
